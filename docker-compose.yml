version: "3.8"

services:
  # Note: This project now uses Qdrant Cloud instead of local Milvus
  # Remove the following services as they are no longer needed:
  # - milvus-etcd
  # - milvus-minio
  # - milvus-standalone

volumes:
  # Note: No local volumes needed for Qdrant Cloud
